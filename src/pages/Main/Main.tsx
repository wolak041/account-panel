import React from 'react';

import { Box, Typography } from '@mui/material';

export const MainPage = () => (
    <Box>
        <Typography sx={{ fontWeight: 'bold', fontSize: 24 }}>Historia</Typography>
    </Box>
);
